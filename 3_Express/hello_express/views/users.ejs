<html>
<body>
    <h2>Here are all the users:</h2>
    <% for (var x in users) { %> 
        <h3>Name: <%= users[x].name %></h3>
        <h4>Email: <%= users[x].email %></h4>
        <hr>
    <% } %>
    <!-- Notice that <% %> is for JavaScript Code
    and the <%= %> is for what is printed to the document.
    use the <% %> for loops and logic. -->
</body>
</html>